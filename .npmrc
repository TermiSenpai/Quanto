# pnpm: hoist node_modules flat so electron-builder and electron resolve
# native/binary deps the same way they did under npm. Quanto ships a
# Windows installer; a non-hoisted (symlinked) layout has historically tripped
# electron-builder's dependency collection. Keep hoisted until proven safe.
node-linker=hoisted
