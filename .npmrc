# pnpm 11 reads ONLY auth/registry settings from .npmrc; every other setting
# moved to pnpm-workspace.yaml. The hoisting that electron-builder needs lives
# there now as `nodeLinker: hoisted` (a `node-linker=hoisted` line here would be
# silently ignored). Keep this file for registry/auth config only.
